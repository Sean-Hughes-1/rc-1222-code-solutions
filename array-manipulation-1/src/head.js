/* exported head */

/* Create a function that accepts an array as an argument which finds the first
element in the array and returns that value

function head(array) {
  return the first element in the array
}
*/
function head(array) {
  return array[0];
}

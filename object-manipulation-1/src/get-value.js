/* exported getValue */
/* Create a function that takes two arguments, an object and a key,
which returns the value of the given key for the object.
function getValue(object, key) {
  return value of given key from object
}
*/
function getValue(object, key) {
  return object[key];
}
